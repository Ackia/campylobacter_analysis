profiles {
    standard {
        executor = 'local'
        docker.enabled = true
    }
    lmod {
        executor = 'local'
        docker.enabled = false
        process {
            module = 'abacas:fastqc:multiqc:megahit:mlst/2.10'
        }
    }
}
